---
const userLang =
  Astro.request.headers.get("accept-language")?.split(",")[0].split("-")[0] ||
  "en";
const defaultLang = userLang === "fr" ? "fr" : "en";
---

<meta http-equiv="refresh" content={`0;url=/${defaultLang}`} />
